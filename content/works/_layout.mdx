---
type: work
---
<Container>

[How to navigate this repository](https://nationalgalleryofart.github.io/accademia-research/)

</Container>

import {
  Label,
  Summary,
  Metadata,
  RequiredStatement,
  References,
} from "@canopy-iiif/app/ui/server";

<Container variant="wide" style={{display: "flex", gap: "2.618rem"}}>
  <div>
    <header>
      <Label manifest={props.manifest} as="h1" />
      <Summary manifest={props.manifest} as="p" className="canopy-lead" />
    </header>
    <div>
      <References />
      <Metadata manifest={props.manifest} />
      <RequiredStatement manifest={props.manifest} />
    </div>
  </div>
  <div style={{width: "60%", flexShrink: 0, marginTop: "3rem"}}>
    <Viewer
      iiifContent={props.manifest.id}
      options={{
        informationPanel: {
          open: true,
          renderAbout: false,
          renderAnnotation: true,
          defaultTab: "manifest-annotations",
        },
        annotations: {
          motivations: ["tagging"],
        },
      }}
    />
  </div>
</Container>
<Container variant="wide">
  <Scroll
    iiifContent={props.manifest.id}
    options={{
      offset: 16,
      figure: {
        aspectRatio: 1,
        display: "image-viewer",
        width: "650px",
      },
      annotations: {
        motivations: ["commenting"],
      },
    }}
  />
</Container>
<Container>
  <h2>Related Items</h2>
  <RelatedItems iiifContent={props.manifest.id} top={3} />
</Container>
